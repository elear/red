{
  "name": "rfced-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --env staging",
    "dev": "wrangler dev --env staging",
    "dev-remote": "wrangler dev --env staging --remote"
  },
  "devDependencies": {
    "oxfmt": "0.45.0",
    "oxlint": "1.60.0",
    "wrangler": "4.43.0"
  },
  "dependencies": {
    "cookie": "1.0.2",
    "itty-router": "5.0.22",
    "rfc4648": "1.5.4"
  }
}
